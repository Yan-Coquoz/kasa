import React from "react";

import "./style.scss";

const Footer = (props) => {
  // écrire le code ici
  return (
    <div>
      <div>logo</div>
      <div>les droits</div>
    </div>
  );
};

export default Footer;
