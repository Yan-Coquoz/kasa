import React from "react";

import "./style.scss";

const Identity = () => {
    // écrire le code ici
    return (
        <div>
            <h3>identity</h3>
            <span>stars</span>
        </div>
    );
};

export default Identity;
